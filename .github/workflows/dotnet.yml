name: .NET

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
    inputs:
      TAG:
        required: true
      

jobs:

  setup_env:
    runs-on: ubuntu-latest
    outputs:
      TAG: ${{ steps.set-output-defaults.outputs.TAG }}
    steps:
      - name: set outputs with default values
        id: set-output-defaults
        run: |    
          # If workflow_dispatch, use inputs (left), if other trigger, use default env (right)
          echo "::set-output name=TAG::$GITHUB_SHA"
      - name: Test
        run: echo $TAG
    
  build:
    uses: itsbibeksaini/github-actions-workflows/.github/workflows/dotnet-build.yml@main
    needs: setup_env
    with:
      SOLUTION_FILE: ./IdentityApi.sln
      PROJECT_FILE: ./IdentityApi/IdentityApi.csproj
      TEST_PROJECT_FILE: ./IdentityApiTest/IdentityApiTest.csproj

#   compute_tag:
#     runs-on: ubuntu-latest
#     needs: build
#     steps:
#       - name: Set env
#         run: echo "GITHUB_SHA_SHORT=$(echo $GITHUB_SHA | cut -c 1-6)" >> $GITHUB_ENV
#       - name: Test
#         run: echo $GITHUB_SHA_SHORT
        
  image:
    uses: itsbibeksaini/github-actions-workflows/.github/workflows/docker-build.yml@main
    needs: build
    with:
      DOCKERFILE: "IdentityApi/Dockerfile"
      IMAGE_NAME: "identityapi"
      TAG: ${{needs.EnvSetup.outputs.TAG}}
  push:
    uses: itsbibeksaini/github-actions-workflows/.github/workflows/push-docker-image.yml@main
    needs: image
    secrets:
      DIGITALOCEAN_ACCESS_TOKEN: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}
  deploy:
    uses: itsbibeksaini/github-actions-workflows/.github/workflows/helm-deploy.yml@main
    needs: push
    secrets:
      DIGITALOCEAN_ACCESS_TOKEN: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}
#     - uses: actions/checkout@v3
#     - name: Setup .NET
#       uses: actions/setup-dotnet@v2
#       with:
#         dotnet-version: 6.0.x
#     - name: Restore dependencies
#       run: dotnet restore ./IdentityApi.sln
#     - name: Build
#       run: dotnet build ./IdentityApi/IdentityApi.csproj --no-restore
#     - name: Test
#       run: dotnet test ./IdentityApiTest/IdentityApiTest.csproj --verbosity normal
