name: .NET

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    uses: itsbibeksaini/github-workflows/.github/workflows/dotnet-build.yml@main
    with:
      SOLUTION_FILE: ./IdentityApi.sln
      PROJECT_FILE: ./IdentityApi/IdentityApi.csproj
      TEST_PROJECT_FILE: ./IdentityApiTest/IdentityApiTest.csproj
      PROJECT_NAME: IdentityApi
  
  image:
    uses: itsbibeksaini/github-workflows/.github/workflows/docker-build.yml@main
    needs: build
    with:
      DOCKERFILE: "IdentityApi/Dockerfile"
      IMAGE_NAME: "identityapi"
      PROJECT_NAME: IdentityApi
  
  push:
    uses: itsbibeksaini/github-workflows/.github/workflows/push-docker-image.yml@main
    needs: image
    with:
      IMAGE_NAME: "identityapi"
    secrets:
      DIGITALOCEAN_ACCESS_TOKEN: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}
  
#   image:
#     uses: itsbibeksaini/github-actions-workflows/.github/workflows/docker-build.yml@main
#     needs: build
#     with:
#       DOCKERFILE: "IdentityApi/Dockerfile"
#       IMAGE_NAME: "identityapi"
#       TAG: $(echo $GITHUB_SHA | head -c7)
#     - uses: actions/checkout@v3
#     - name: Setup .NET
#       uses: actions/setup-dotnet@v2
#       with:
#         dotnet-version: 6.0.x
#     - name: Restore dependencies
#       run: dotnet restore ./IdentityApi.sln
#     - name: Build
#       run: dotnet build ./IdentityApi/IdentityApi.csproj --no-restore
#     - name: Test
#       run: dotnet test ./IdentityApiTest/IdentityApiTest.csproj --verbosity normal
