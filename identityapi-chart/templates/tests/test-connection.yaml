apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "identityapi-chart.fullname" . }}-test-connection"
  labels:
    {{- include "identityapi-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "identityapi-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
