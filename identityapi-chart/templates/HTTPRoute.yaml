apiVersion: gateway.networking.k8s.io/v1alpha2
kind: HTTPRoute
metadata:
  name: identityapi
spec:
  parentRefs:
  - name: tmsolution-gateway
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - kind: Service
      name: identityapi
      port: 8080
      weight: 1000
